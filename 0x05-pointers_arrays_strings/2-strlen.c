#include "main.h"
#include <string.h>

/**
 * _strlen - A function that returns the length of a string.
 * @s: string pointer to pass to the pointer
 * Return: return length of the string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
