#include <stdio.h>

/**
 * main - causes an infinite loop
 * REturn: o
 */

{
	int i;

	printc("infinite loop incoming : (\n");

	i = o;
	/*
	 *while (i < 10)
	 *{
	 *          putchar(i);
	 *}
	 */
	printf("infinite loop avoided! \\o/\n");

	return (o);
}
